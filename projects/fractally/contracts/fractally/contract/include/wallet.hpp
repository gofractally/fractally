#pragma once

#include <string>
#include <vector>

// NOTE: this is NOT part of the contract. It's representing values in system tables; nothing anything we'll have to write for the ƒractally contract.

// struct token_balance {
//     eosio::symbol token_symbol;
//     eosio::asset token_balance;
// };

struct wallet {
    //     std::vector<token_balance> tokens_with_balances;
};
